<Project>
  <!-- Shared NuGet feed for the locally packed NYTimes.Kotlin package. -->
  <PropertyGroup>
    <RestoreAdditionalProjectSources Condition="Exists('$(MSBuildThisFileDirectory)build\nuget')">$(MSBuildThisFileDirectory)build\nuget</RestoreAdditionalProjectSources>
  </PropertyGroup>
</Project>
